# ============================================
# Frontend Environment Configuration
# ============================================
# Copy this file to .env.local and adjust values as needed

# --------------------------------------------
# Backend API Configuration (Required)
# --------------------------------------------
# The base URL of the backend reservation API
# This MUST use NEXT_PUBLIC_ prefix to be accessible in client components
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# --------------------------------------------
# Optional: Development Settings
# --------------------------------------------
# Uncomment to override default behaviors

# API request timeout in milliseconds (default: 30000)
# NEXT_PUBLIC_API_TIMEOUT=30000

# Enable/disable React Query devtools (default: true in dev)
# NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=true

# Default user ID for demo purposes (default: demo-user)
# NEXT_PUBLIC_DEFAULT_USER_ID=demo-user

# --------------------------------------------
# Notes:
# --------------------------------------------
# - Never commit .env.local files to version control
# - This .env.example file IS safe to commit
# - Keep backend running on port 3000 for default config
# - Frontend runs on port 3001 by default
